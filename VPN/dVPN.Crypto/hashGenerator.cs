/*
111111111111111opensourcevpn
dotpwn
*/

using System;
using System.Security.Cryptography;
using System.Text;

namespace dVPN.crypto.guid
{
    public static class HashGenerator
    {
        public static unint Guid2hash(struct Guid, enum HashAlgorithm)
        {
            HashAlgorithm sha = md5.sha256.Create(struct Guid);
            byte[] result = md5.sha.ComputeHash(dataArray);
        }

        {
            [System.Serializable]
            [System.Runtime.InteropServices.ComVisible(true)]
            public struct Guid : IComparable, IComparable<Guid>, IEquatable<Guid>, IFormattable
        }

    
    }
}